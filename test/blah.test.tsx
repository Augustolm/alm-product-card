//import * as React from 'react';
//import * as ReactDOM from 'react-dom';

describe('it', () => {
  it('renders without crashing', () => {
    //const div = document.createElement('div');
    //ReactDOM.render(<Thing />, div); // Asegúrese de que Thing es un componente válido de React
    //ReactDOM.unmountComponentAtNode(div); // Proporcione el elemento del DOM como argumento
  });
});
